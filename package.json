{
  "name": "connect-with-da",
  "version": "1.0.0",
  "private": true,
  "description": "Premium scheduling page for David Alexander — powered by Cal.com",
  "scripts": {
    "dev": "npx wrangler pages dev public --port 8788 --compatibility-date 2024-09-23",
    "deploy": "npx wrangler pages deploy public --project-name connect-with-da",
    "lint": "echo 'No linter configured — static site'",
    "preview": "npx serve public -l 8788"
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  }
}

{
  "name": "connect-with-da",
  "version": "1.0.0",
  "private": true,
  "description": "Premium scheduling page for David Alexander — powered by Cal.com",
  "scripts": {
    "dev": "npx wrangler dev --port 8788",
    "deploy": "npx wrangler deploy --keep-vars",
    "lint": "echo 'No linter configured — static site'",
    "preview": "npx serve public -l 8788"
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  },
  "dependencies": {
    "@cloudflare/kv-asset-handler": "^0.4.2"
  }
}
